{
  "name": "playwright-agent-demo-learnwebdriverio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "prettier": "prettier --write '**/*.{js,ts}'"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^18.0.0",
    "prettier": "3.8.0"
  }
}
